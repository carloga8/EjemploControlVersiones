# Perimetro de un circulo
perim_circulo <- function(r) {return(2*3.14159*r)}

# Perimetro de un rectangulo
perim_rectangulo <- function(L, l) {return(2*L+2*l)}

# Area de un trapecio
area_trapecio <- function(b, B, H) {
  return((b + B) * H / 2)
}
